@startuml
start

:Open System Start Page;
:Choose Add Database System;

:Enter System Details (Name, Password, Configuration);
:Validate Connection Details;

if (Configuration Valid) then (true)
    :Register Database System with Middleware;
    :Success Message;
else (false)
  :Error Message;
endif
stop
@enduml