@startuml

actor User
participant "Database" as db
participant "Configuration Validator" as validator

User -> db : Select Database
User -> db : Choose Consistency Strategy
User -> validator : Validate Configuration
alt Configuration Valid?
    validator -> db : Apply Consistency Strategy
    db -> User : Success Message
else
    validator -> User : Error Message
end

@enduml
